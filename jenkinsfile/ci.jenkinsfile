#!groovy
String buildShell = "${env.buildShell}"
pipeline{
    agent { node {label "master"}}
    stages{
        steps{
            script{
                mvnHome = tool "M2"
                sh "${mvnHome}/bin/mnv ${buildshell}"
            }
        }
    }
}
